function calculaCampo(){
  // declaracoes de variaveis
  var campoA;
  var campoB;

  // recebendo os valores do meus input do forms
  var campoA= document.getElementsById('campo-A'); 
  var campoB= document.getElementById('campo-B'); 

  // variável que vai ficar responsavel por comparar os dois 
  comparCampos = campoA + campoB; 
  if(compoB < compoA) {
    
  }

  
}

